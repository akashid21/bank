hello to transfer 

= form_with url: users_execute_transfer_path, method: :post, local: true do |f|
  = f.label :to_account_number
  = f.number_field :to_account_number, autocomplete: 'off'

  = f.label :ammount
  = f.number_field :ammount, autocomplete: 'off'

  = f.submit