.div{style: 'background-color: pink; height: 300px; width: 300px'}
  - if current_user.image.attached?
    = image_tag(current_user.image, style: 'height: 200px; width: 200px; border-radius: 50%; margin: 10%; margin-left: 20%')
= form_with model: @avatar, url: users_upload_avatar_path, method: :put do |f|

  = f.label :image
  = f.file_field :image

  = f.submit 'Upload'